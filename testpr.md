# Test PR marker

Throwaway file for a fresh test PR under issue #106, created to have something
open to exercise (e.g. commenting `/rebase` to verify
`.github/workflows/rebase-branch.yml`). Safe to close/delete without merging.
